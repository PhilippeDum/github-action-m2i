name: "03 - PR"

on:
  pull_request:
    branches:
      - main

jobs:
  afficher-info:
    runs-on: ubuntu-latest
    steps:
      - name: "Afficher des infos pr"
        run: |
          echo "PR : "
          echo "Author : ${{ github.actor }}"
          echo "Branch pr : ${{ github.head_ref }}"
          echo "Cible : ${{ github.base_ref }}"